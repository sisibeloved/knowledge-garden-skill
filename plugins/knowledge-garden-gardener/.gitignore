# Obsidian 本地状态(不进 Git,避免多机冲突)
.obsidian/workspace*.json
.obsidian/plugins/*/data.json
*.cache
_Trash/

# garden init 运行时产物(checkpoint + 内部状态),不应入库
.garden-init.json
# Tests:临时样本 / checkpoint 不应入库
tests/.garden-*
# capture 运行时误写到 plugin 目录的真实 Raw(模板只保留 .gitkeep)
_System/_Inbox/raw-*.md
